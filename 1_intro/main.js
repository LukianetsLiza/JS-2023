// ------------- browser dialogs -----------
alert("Some alert message!");

// -------------- 1 --------------------
const userage = prompt('Enter your year of birth:');
alert(`Your age ${2023-userage}!`);

// -------------- 2 --------------------
const radius = prompt('Enter circle radius:');
alert(`Area of ​​a circle ${2*3.14*radius}!`);

// -------------- 3 --------------------
const distance = prompt('Distance between two cities:');
const time = prompt('The time you have to reach your destination:');
alert(`The speed at which you must move ${distance/time}!`);

// -------------- 4 --------------------
const dollars = prompt('Enter dollars:');
alert(`It will be ${dollars*0.94} euros!`);

// -------------- 5 --------------------
const capacity = prompt('Enter the capacity of the flash drive:');
alert(`${Math.floor(capacity*1024/820)} files with a size of 820 MB can be saved on a flash drive!`);